<%= form_with(model: post1, local: true) do |form| %>
  <% if post1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post1.errors.count, "error") %> prohibited this post1 from being saved:</h2>

      <ul>
        <% post1.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :types %>
    <%= form.text_field :types %>
  </div>

  <div class="field">
    <%= form.label :lostdate %>
    <%= form.date_select :lostdate %>
  </div>

  <div class="field">
    <%= form.label :lastlocation %>
    <%= form.text_area :lastlocation %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
 

 
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
